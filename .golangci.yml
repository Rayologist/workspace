version: "2"
linters:
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint
        - (*github.com/spf13/cobra.Command).MarkFlagRequired
